# Full local stack: everything, including the heavy optional pieces.
# Use this on your own machine when you want Ollama (free, offline) and the
# local cross-encoder reranker:
#     pip install -r requirements-local.txt
# Streamlit Community Cloud must NOT use this file (~2 GB of torch); it installs
# requirements.txt instead.

# Core RAG
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
faiss-cpu>=1.8
pypdf>=4.2
unstructured>=0.15
markdown>=3.6

# Hybrid retrieval + reranking
rank-bm25>=0.2.2
sentence-transformers>=3.0        # dense embeddings + cross-encoder reranker

# Backends (install what you use)
langchain-openai>=0.2
langchain-google-genai>=2.0            # free tier: chat + embeddings, no card
langchain-ollama>=0.2
langchain-huggingface>=0.1

# App + API
streamlit>=1.37
fastapi>=0.111
uvicorn>=0.30

# Config + dev
pydantic-settings>=2.3
python-dotenv>=1.0
pytest>=8.0
ruff>=0.5
